---
description:
globs:
alwaysApply: false
---
# Development Setup Guide

## Prerequisites
- Node.js (Latest LTS version recommended)
- npm (comes with Node.js)
- Git

## Getting Started

1. Clone the repository
2. Install dependencies:
   ```bash
   npm install
   ```

## Available Scripts

From [package.json](mdc:package.json), the following scripts are available:

- `npm run dev` - Start development server
- `npm run build` - Build for production
- `npm run start` - Start production server
- `npm run lint` - Run ESLint

## Configuration Files

- [tsconfig.json](mdc:tsconfig.json) - TypeScript configuration
- [next.config.ts](mdc:next.config.ts) - Next.js configuration
- [eslint.config.mjs](mdc:eslint.config.mjs) - ESLint rules
- [postcss.config.mjs](mdc:postcss.config.mjs) - PostCSS and Tailwind configuration

## Development Workflow

1. Start the development server:
   ```bash
   npm run dev
   ```

2. The application will be available at `http://localhost:3000`

3. Make changes to files in `src/app/`:
   - Changes to `page.tsx` will update the main page
   - Changes to `layout.tsx` will update the root layout
   - Changes to `globals.css` will update global styles

4. Use ESLint to check code quality:
   ```bash
   npm run lint
   ```

## Project Structure
See [project-structure.mdc](mdc:.cursor/rules/project-structure.mdc) for detailed information about the project structure.

## Next.js App Router
See [nextjs-app-router.mdc](mdc:.cursor/rules/nextjs-app-router.mdc) for information about the Next.js App Router pattern used in this project.
